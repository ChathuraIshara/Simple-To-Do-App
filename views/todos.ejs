<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@4.1.3/dist/css/bootstrap.min.css" integrity="sha384-MCw98/SFnGE8fJT3GXwEOngsV7Zt27NXFoaoApmYm81iuXoPkFOJwJ8ERdknLPMO" crossorigin="anonymous">
    <title>To-Do-App</title>
</head>
<body>
    <div class="container">
        <div class="text-center">
            <h1 class="md-5">Daily Tasks!</h1>
                <form method="post" action="/todo" class="mt-4">
                    <input type="text" name="task" class="form-control" placeholder="Enter your task"><br>
                    <input type="submit" class="btn btn-primary" value="Save">
                    <input type="button" class="btn btn-danger" value="Cancel">
                </form>
                <table class="table table-dark mt-5">
                    <tr>
                        <th>Task-Name</th>
                        <th>Completed</th>
                        <th colspan="3">Actions</th>
                    </tr>
                    <% for(var i=0; i< todos.length;i++) {%>
                        <tr>
                            <td><%= todos[i].item %></td>
                            <%if(todos[i].isCompleted){%>
                                <td><button class="btn btn-success">Completed</button></td>
                            <%}else{%>
                                <td><button class="btn btn-warning">Not-Completed</button></td>
                               <%}%>
                               <%if(todos[i].isCompleted){%>
                                <td><a href="/todo/marknotcompleted/<%= todos[i]._id%>"><button class="btn btn-danger">Mark as not Completed</button></a></td>
                            <%}else{%>
                                <td><a href="/todo/markcompleted/<%= todos[i]._id%>"><button class="btn btn-primary">Mark as Completed</button></a></td>
                               <%}%>  
                            <td><a href="/todo/delete/<%= todos[i]._id%>" class="btn btn-danger">Delete</a></td>
                            <td><a href="/todo/updatepage/<%= todos[i]._id%>" class="btn btn-warning">Update</a></td>
                            
                           
                        </tr>
                    <% }%>
                   
                </table>
               
            </div>
        </div>
    </div>
    
</body>
</html>